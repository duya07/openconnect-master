# OpenConnect VPN 账户配置示例
#
# 格式:
# 显示名|用户名|密码|VPN主机|认证组(可空)|协议(anyconnect/nc/pulse)
#
# 注意:
# - 这是 root-only 的明文凭据文件，不是加密存储。
# - 字段不能包含 |，密码不能包含换行。
# - 正式文件路径为 /root/.vpn_accounts.env，必须 chmod 600。
# - 不要把真实凭据提交到 Git。

# Juniper Network Connect 兼容网关
学校 VPN|student001|replace-me|https://vpn.university.example||nc

# Cisco AnyConnect 网关
公司 VPN|employee@example.com|replace-me|https://vpn.company.example|Employees|anyconnect

# Pulse / Ivanti 网关
合作方 VPN|partner001|replace-me|https://pulse.example.net|Users|pulse
